<h1>ジャンル一覧・追加</h1>
<%= form_with model: @genre,url: admin_genres_path, method: :post do |f| %>
<div>ジャンル名 <%= f.text_field :name%></div>
<%=f.submit '新規登録'%>
<% end %>


<h2>ジャンル一覧</hs>
<% @genres.each do |g|%>
<div>
<%=g.name%><%=link_to '編集する' %>
</div>
<% end %>
